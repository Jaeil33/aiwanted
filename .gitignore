node_modules/
.next/
out/
next-env.d.ts
tsconfig.tsbuildinfo

# phase execution outputs
phases/**/phase*-output.json
phases/**/step*-output.json

# personal materials (자소서) — keep out of git
/네이버 클라우드 자소서 작성/
/네이버 클라우드 자소서 작성.zip

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# data: Naver/Open-Meteo raw caches and pipeline intermediates are never committed (rights + size).
# Only the app bundle inputs in data/build/app are committed so Vercel's GitHub build has them (ADR-023).
/data/*
!/data/build/
/data/build/*
!/data/build/app/

# build outputs
dist/
dist-artifact/
coverage/
*.local

# local secrets & personal settings
.env
.env.*
.claude/settings.local.json

# isolated agent worktrees
.claude/worktrees/

# vercel cli link & local build output
.vercel
